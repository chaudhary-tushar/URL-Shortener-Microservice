apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: auth-ingress
  labels:
    name: auth-ingress
spec:
  rules:
  - host: tri.me
    http:
      paths:
      - pathType: Prefix
        path: "/"
        backend:
          service:
            name: auth
            port: 
              number: 5000